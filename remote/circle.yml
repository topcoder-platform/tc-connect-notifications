machine:
  node:
    version: v6.9.0
  services:
    - docker

dependencies:
  post:
    - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$AWS_REPOSITORY:$CIRCLE_SHA1 .


deployment:
  prod:
    branch: master
    commands:
      - chmod +x ./deploy.sh
      - ./deploy.sh
